# meow:multimodal Environment Variables
#
# RECOMMENDED: Set all MEOWKIT_* vars in .claude/.env (central config).
# This skill-specific .env is for overrides only — it loads AFTER .claude/.env.
#
# See .claude/.env.example for the full list of MEOWKIT_* variables:
#   API keys, provider chains, model overrides, video resolution, etc.
#
# Skill-specific overrides (uncomment to override central config):
# MEOWKIT_IMAGE_GEN_MODEL=gemini-3.1-flash-image-preview
