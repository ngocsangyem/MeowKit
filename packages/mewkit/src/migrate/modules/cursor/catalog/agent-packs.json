{
	"_description": "Cursor bundle agent-pack catalog. `core` is the default install (Phase 3 part A) — explorer, planner, reviewer. Everything else stays an inactive/empty pack placeholder until the full catalog (codex parity) lands in a later phase, per the plan's 'small default install, packs opt-in' decision.",
	"verified": "2026-07-24",
	"defaultPack": "core",
	"packs": {
		"core": {
			"description": "Default install: fast read-only repo orientation (explorer), architecture/implementation planning (planner), evidence-ranked post-implementation review (reviewer). Kept small per Cursor's own guidance against many vague-description agents degrading auto-routing quality.",
			"dependsOn": [],
			"agents": ["explorer", "planner", "reviewer"]
		}
	}
}
