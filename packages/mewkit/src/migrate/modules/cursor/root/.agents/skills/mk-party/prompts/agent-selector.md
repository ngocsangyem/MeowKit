# Agent Selection for Party Mode

The canonical agent-selection table and selection constraints live in `SKILL.md` "1. Agent Selection".

This file exists for filesystem-layout consistency with sibling skills (`plan-creator`, `evaluate`, `review`). Do not duplicate the table or constraints here — keep SKILL.md as the single source of truth.
