# Usage

<!-- ref: inline-unmapped-runtime -->
Session state persists under .claude/memory/demo-skill.json.

<!-- ref: fenced-runtime-command (mapped) -->
```bash
python3 .claude/skills/demo-skill/scripts/run.py --verbose
```

<!-- ref: claude-md-token-prose -->
Record decisions in CLAUDE.md when the workflow changes.
