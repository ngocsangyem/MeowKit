export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sky',    // closest built-in to our #66CCFF magic-glow cyan
      neutral: 'slate',  // matches our --color-muted slate palette
    },
    button: {
      defaultVariants: {
        size: 'lg',
      },
    },
  },
})
