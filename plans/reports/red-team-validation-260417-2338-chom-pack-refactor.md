# Red-Team Validation Report — chom + pack Post-Refactor

**Date:** 2026-04-17 23:38
**Targets:** `meow:chom` (refactored), `meow:pack` (new + --compress added)
**Method:** 5 specialized teams, evidence-based, no "it should work" assumptions.
**Verdict:** 1 CRITICAL hallucination fixed, 2 MODERATE gaps fixed, 2 LOW polish items fixed. Both skills validated end-to-end.

---

## Team 1 — Execution Flow

Walked 4 chom scenarios + 2 pack scenarios:

| Scenario | Result |
|---|---|
| `/meow:chom facebook/react state-mgmt --improve` | ✓ flow traces cleanly: clone → scout → Phase 3 with anti-pattern focus → HARD GATE → handoff |
| `/meow:chom vendor/lib` (no flag) | ✓ full workflow, handoff without mode declaration |
| `/meow:chom vendor/lib --compare` | ✓ phases 1–3, Comparison Report, STOP |
| `/meow:chom yamadashy/repomix --lean` | **F3 found:** `--lean` skips researcher-agent step, but researcher runs only for freeform inputs per routing table. For git-URL/local inputs, `--lean` is no-op. Fix applied — semantics now documented per input type. |
| `/meow:pack yamadashy/repomix` | ✓ smoke test earlier produced 4.36 MB markdown |
| `/meow:pack yamadashy/repomix --compress` | ✓ smoke test produced 3.2 MB compressed (26% smaller, signatures only) |

---

## Team 2 — Integration & Wiring

Verified every cross-reference:

| Referenced | Exists? |
|---|---|
| `meow:scout` | ✓ |
| `meow:web-to-markdown` | ✓ |
| `meow:browse` | ✓ |
| `meow:multimodal` | ✓ |
| `meow:sequential-thinking` | ✓ |
| `meow:plan-creator` | ✓ |
| `meow:pack` | ✓ |
| `researcher` agent | ✓ (`.claude/agents/researcher.md`) |
| `references/challenge-framework.md` | ✓ 122 lines, untouched |
| `scripts/self-pack-guard.sh` | ✓ executable |

**F2 finding:** Both `meow:chom` and `meow:pack` missing from `.meowkit.manifest.json` (41 skills registered; disk has more — manifest is stale-by-design, regenerated by release tooling). Also missing: `meow:brainstorming`, `meow:angular`, others. **Not a functional blocker** — skills are discovered from `.claude/skills/*/SKILL.md` directly, not from manifest.

No hook intercepts chom/pack execution (verified `grep -rln 'meow:chom\|meow:pack' .claude/hooks/` → empty). No orphaned triggers.

---

## Team 3 — Context & Memory

| Check | Result |
|---|---|
| chom SKILL.md loaded at session start | 187 lines ≈ 4K tokens |
| Lazy-loaded on Phase 4 | `references/challenge-framework.md` (~2-3K tokens) |
| Memory usage | None (no persistence per spec) |
| Over-context risk | None — all references justified |

Total context budget for full chom workflow: ~7K tokens skill content + workflow output. Reasonable.

---

## Team 4 — Reality & API Grounding (CRITICAL)

**F1 — Hallucination found and fixed:**

**Claim (before fix):** "Skills cannot call skills (MeowKit convention)."

**Evidence against:**
- `grep -rin -E 'skill.*skill' .claude/rules/*.md` → 0 matches for the prohibition.
- `CLAUDE.md` → no such rule.
- `docs/research/lessons-build-skill.md` §Composing Skills explicitly says: *"you can just reference other skills by name, and the model will invoke them if they are installed."*
- The rule originated in xia SKILL.md line 124 as a **xia-specific phase-ownership choice**, and in chom's pre-existing gotcha as a **chom-specific handoff pattern** — neither is a MeowKit platform rule.

**Fix applied:** replaced "Skills cannot call skills (MeowKit convention)" with:
> "This is chom's design choice, not a MeowKit platform rule. Skills *can* reference other skills... chom opts out because mid-flow invocations of other orchestration skills would break phase ownership."

**Also fixed:** Gotchas line that repeated the hallucination.

**Other claims verified:**
- `npx repomix@^1.11` works — smoke test confirmed earlier (4.36 MB output)
- `--compress` works — smoke test confirmed today (3.2 MB output)
- Secretlint is repomix's scanner — **attributed, not independently audited** (documented in pack SKILL.md gotchas)
- Tree-sitter signature extraction — confirmed via `--compress` output's `⋮----` delimiter between signatures

---

## Team 5 — Failure & Edge Cases

| Injected failure | Behavior | Result |
|---|---|---|
| Source unreachable (bad URL) | F4: no Error Recovery entry existed — **fix applied** (new line: "Source empty / unreachable / invalid → stop, report what was attempted, ask user to verify") | ✓ |
| `git clone` fails (private repo) | Existing: "Source private/missing → ask for access or alternative" | ✓ |
| `--self` passed to chom (wrong flag) | chom doesn't list `--self` — flag would be ignored; not a security issue since `--self` is only meaningful to pack | ✓ |
| High risk (≥5) in challenge | Existing: "Risk ≥ 5 → reject with Rejection Report" | ✓ |
| `npx` offline (first run) | Documented in pack gotchas | ✓ |
| `--compare` output location unclear | F5: output location undefined — **fix applied** ("Print to conversation; do not write to disk unless user explicitly asks") | ✓ |

---

## Fixes Applied

| ID | File | Change |
|---|---|---|
| **F1** | `meow:chom/SKILL.md` §Boundary Rules + Gotchas | Removed "Skills cannot call skills (MeowKit convention)" hallucination; replaced with honest "chom's design choice" framing |
| **F3** | `meow:chom/SKILL.md` §Phase 1 Recon | Documented `--lean` semantics per input type (freeform = skip researcher; git/local/web/image = no-op) |
| **F4** | `meow:chom/SKILL.md` §Error Recovery | Added "Source empty / unreachable / invalid" handling |
| **F5** | `meow:chom/SKILL.md` §Output: Comparison Report | Added "Print to conversation; do not write to disk unless user explicitly asks" |

F2 (missing from manifest) **not fixed** — it's a build-time artifact regenerated by release tooling; not a functional blocker; affects many other skills too; out of scope for this refactor.

## Final State

### meow:chom

- 187 lines SKILL.md (under 200 budget)
- 4 modes user-explicit, 2 speed flags
- HARD GATE non-bypassable (verified 3 grep matches)
- Security anchor intact
- Challenge framework file untouched
- All cross-references verified to exist
- No hallucinated rules remaining

### meow:pack

- 96 lines SKILL.md (under 150 threshold)
- `--compress` flag exposed and smoke-tested
- "40–70% context burn" fabrication replaced with correct "context-isolation" framing
- self-pack-guard.sh: 3/3 test scenarios pass
- Smoke tests: both `--style markdown` and `--compress` produce valid output

### Diff Totals

```
.claude/skills/meow:chom/SKILL.md | 96 ++++++++++++++++++++++++++++++---------
.gitignore                        |  1 +
.gitignore.meowkit                |  1 +
3 files changed, 76 insertions(+), 22 deletions(-)
```

Plus untracked: `.claude/skills/meow:pack/` (4 files).

---

## Remaining Risks

| Risk | Severity | Justified? |
|---|---|---|
| F2: manifest stale | Low (cosmetic) | Yes — build artifact, affects many skills, not this refactor's concern |
| Secretlint scanner attribution | Low | Yes — documented as "sourced from repomix docs, not independently audited" |
| chom line count growing near 200 on future edits | Low | Yes — v1.1 step-file decomposition queued per plan |
| Mode recommendation removed — plan-creator gets less signal | Low-Medium | Yes — per user decision to remove auto-derivation; trade-off accepted |

## Unresolved Questions

1. Should a manifest-regeneration script be run to register chom + pack (and fix drift for other missing skills)? **Out of scope for this refactor**; separate concern.
2. Should pack's `--compress` be mentioned in `meow:scout` gotchas as a pointer for API-surface queries? **Could add**; currently only chom gotchas reference it.
